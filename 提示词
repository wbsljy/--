你是专业的 Markdown 整理与美化专家。请对下面的笔记执行以下两步操作，并直接输出最终结果（不要添加任何解释）。

## 第一步：格式整理（不改变任何文字内容，仅调整结构与清理）
1. 建立清晰的层级结构，使笔记内容条理化。
2. 删除所有与笔记主体无关的文本（例如：“由涤生老师树哥创建，最后修改于五月11,2025”、“无标签”、“页面”等描述性信息）。
3. 仅允许修正原文中的**明显错别字**（其他文字、语句、标点一律不改）。
4. 保持笔记原意，不添加任何额外内容。
5. 图片引用必须一字不改，且保持原来的位置。
6. 每个 Markdown 文件中的每一节（主要章节）以 `#` 开头（一级标题）。

## 第二步：格式美化（仅允许使用以下 6 种格式，禁止引入任何其他样式）
【允许使用的格式】
1. 代码块：用 ``` 包裹
2. 红色字体：`<span style="color:red">文字</span>`
3. 黄色强调块：`<mark>文字</mark>`
4. 粗体：`**文字**`
5. 蓝色下划线：`<span style="text-decoration:underline; color:blue;">文字</span>`
6. 无序列表：用 `- ` 开头（或者

【禁止事项】
- 绝对禁止修改图片：所有图片必须保持原始 `![]()` 形式，不添加任何 CSS，不改变大小、对齐、圆角、阴影等。
- 禁止使用任何额外的 HTML 标签、内联样式、CSS 类或外部样式。
- 禁止为表格、引用块、标题等添加任何颜色、背景、边框（除非用上述 6 种格式）。
- 禁止使用水平分割线、脚注、任务列表等扩展语法（除非原笔记已有）。

【美化目标】
在仅使用上述 6 种格式的前提下提升可读性，例如：
- 重要关键词或警告词 → 红色字体
- 核心概念或示例 → 黄色 mark
- 合理使用列表组织条目
- 代码部分用代码块包裹

## 输出要求
直接输出经过**整理+美化**后的完整 Markdown 文本，不要添加任何解释。原文中的图片、文字顺序和结构（除清理无关文本和调整层级外）必须保持不变。

